import React from 'react';

const ApprovalCard = props => {
    return (
        
            <div float>
                {props.children}
            </div>
            
        

    );

};

export default ApprovalCard;